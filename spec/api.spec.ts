
import * as api from '../src/api';
import {MyClass} from '../src/MyClass';

describe('Public API', () => {
    it('api.spec.ts template', () => {
        expect(api.MyClass).toBe(MyClass);
    });
});
